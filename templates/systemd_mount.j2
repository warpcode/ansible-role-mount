[Unit]
Description=mount {{ item.mount }}
After=network.target multi-user.target

[Mount]
What={{ item.src }}
Where={{ item.mount }}
Type={{ item.fstype }}
{% if item.options is defined %}
Options={{ item.options }}
{% endif %}

[Install]
WantedBy=default.target